# WorldKernel Stage3 Agent-Kernel Project

Generated by `worldkernel.stage3.build_agentkernel_project`.

Run:

```bash
python run_simulation.py --max-ticks 1
```

The generated runtime treats Stage2 LocationProfile artifacts as first-class
environment objects through `WKSpacePlugin`.
